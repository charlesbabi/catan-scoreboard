services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - catan-data:/app/data
    restart: unless-stopped
  frontend:
    build: ./frontend
    ports:
      - "8090:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  catan-data:
